[project]
name = "aeroquery"
version = "0.1.0"
description = "Drone görüntülerinde nesne tespiti + RAG/LLM ile sorgulanabilir hale getirme"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"